#include <stdio.h>

/**
 * main - the program starts
 *
 * Return: - the program ends with 0
 */

int main(void)
{
	int x;

	for (x = 0; x <= 9; x++)
	{
		printf("%d", x);
	}
	printf("\n");

	return (0);
}
