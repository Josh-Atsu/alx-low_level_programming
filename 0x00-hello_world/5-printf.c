#include <stdio.h>

/**
 * main - the starting point of c
 *
 * Return: always 0 and nerver ever
 */

int main(void)
{
	printf("with proper grammar, but the outcome is a piece of art,\n");
	return (0);
}
